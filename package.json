{
  "name": "auto-swiper",
  "version": "3.0.0",
  "description": "Chrome extension that automates swiping on Tinder and Bumble with smart filters, timing, limits, and analytics.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
